{% extends "base.html.twig" %}
{% block layout_content %}
<div class="card border-dark mb-3">
    <h1>Votre profil</h1>
    <div class="card-title">{{ app.artiste.name }}</div>
    <p class="card-text">{{ app.artiste.email }}</p>
</div>
{% endblock %}