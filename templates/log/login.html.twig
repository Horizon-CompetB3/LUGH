{% extends "base.html.twig" %}
{% block layout_content %}


        {% if error %}
            <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}
        {{ form_start(form_1) }}
        {{ form_row(form_1._username) }}
        {{ form_row(form_1._password) }}

        <button type="submit">Connexion</button>
        {{ form_end(form_1) }}

{% endblock %}